repos:
  # Keep this rev and the `ruff` pin in pyproject's dev group on the SAME
  # version. The hook builds its own isolated env, so if the two drift, the
  # pre-commit gate and `uv run ruff` enforce different rule sets — which is
  # exactly what happened between 0.14 and 0.16 (default selection went from
  # 59 rules to 413). Bump both together.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.2
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/HunterMcGushion/docstr_coverage
    rev: v2.3.2
    hooks:
      - id: docstr-coverage
        args: [--verbose, "0", --fail-under=100, src/]
  - repo: local
    hooks:
      - id: gen-catalog
        name: regenerate docs catalog + API reference
        entry: uv run python docs/gen_catalog.py
        language: system
        pass_filenames: false
        files: ^(src/stargazer/(tasks|workflows|assets|bundles)/.*\.py|docs/gen_catalog\.py)$
      - id: zensical-build
        name: zensical build (docs compile check)
        entry: uv run zensical build --clean
        language: system
        pass_filenames: false
        files: ^(docs/.*\.md|src/.*\.py|app/.*\.py|zensical\.toml|pyproject\.toml)$
      - id: mcp-server-import
        name: mcp server import check
        entry: uv run --extra mcp python -c "from stargazer.server import mcp"
        language: system
        pass_filenames: false
        files: ^(src/stargazer/(server|registry|marshal)\.py|src/stargazer/assets/.*\.py|pyproject\.toml)$
